{
  "name": "playonline-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm run dev",
    "build": "rm -rf .wrangler && cd frontend && npm install && npm run build && cd .. && node create-worker.js && rm -rf frontend/node_modules",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "PlayOnline - Full stack application with React frontend and Node.js backend"
}
